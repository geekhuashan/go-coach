# 源码发布验证 · 2026-09-07

本记录只描述此独立源码快照的发布检查，不包含运行档案。

- 导出后的 45 项受影响 Python 单元/HTTP 测试通过：引擎桥接、成对走法复核、本地复核、LLM 路由与迁移工具；使用临时数据目录和测试服务。
- 全部 50 项 Node 测试通过：规则、事务、认证、后端回退、走法复核与视频边界。
- 导出内 Python 文件语法解析通过；前端 app.js 与 Service Worker 的 Node 语法检查通过。
- 可选的全量 Python 测试没有完成：遍历所有题目的 HTTP 测试运行超过三分钟后中断。此前出现的 `test_recommendation_reaches_tactics_after_lower_levels_are_ready` 失败，在原源码与导出副本上独立复现一致（预期难度 5，实际 3），属于已有推荐行为与测试预期不一致，未在此次发布中更改。
- 发布内容经过路径、凭据和 JWT 扫描及语义审阅；GitHub 发布仍需绑定精确提交和本人远端完成检查。

这次只保存源代码分支，没有执行部署、修改生产服务或迁移学习资料。部署配置中的账号、数据库与域名为占位值，使用前须自行配置。

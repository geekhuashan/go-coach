# 第一版验证 · 2026-09-07

- Python 15 项单元测试通过，包含提子、共享气、禁止自杀、劫、悔棋、双停、自定义题、SGF、演示与反馈保留。
- JavaScript `node --check static/app.js` 通过。
- HTTP 实测：健康检查、合法落子、旧 revision 返回 409、异源/恶意 Host 返回 403、失败演示不修改原盘、原子保存。
- Playwright 实际点击 D3，接口读取到黑棋 D3；D4 黑棋连块的气从 1 变为 3。
- 在网页输入并点击保存测试反馈，后端读取到同样文字。
- 保留上述局面与反馈后重启服务，board / moves / feedback / revision 全部一致。
- 教师通过接口开始两手演示；网页显示第一手，点击“演示下一手”后读到第二手；点击“返回原局面”恢复原盘和标记。
- 网页点击“请陪练下一手”，本地 KataGo 为白棋返回落子 F4（此次测试结果，重新计算可能不同），move_number 从 1 变为 2；分析接口返回 8 个候选。
- 620px 宽视窗检查：文档无水平溢出，81 个交叉点均有按钮语义。
- 初始 favicon 404 已通过添加本地图标修复，随后导航没有报告控制台错误。
- 最终状态为第一道 escape 题，测试反馈已随切换题目清空。

界面截图：初始棋盘（本地验证产物 `output/playwright/first-board.png`）。这是早期界面截图，最终版另含逐手演示按钮、棋谱导出链接，以及练习模式禁用停一手。

## 实际边界

KataGo 本地运行可用，当前不是校准弱棋对手。没有自动死活裁决和终局数目，没有网页自动唤醒当前 Codex 对话的连接。电脑重启后的自动启动未配置；启动器手动运行，服务重启后的棋局恢复已验证。

## 第二版验证 · 2026-09-07

- Python 49 项单元测试通过；JavaScript 语法检查通过。覆盖围棋规则、题库、反馈、档案、对局及 LLM 配置与调用路径。
- 64 道练习覆盖逃子、提子、连接与切断；能力调整依据首次独立作答，提示、演示和重复尝试不会重复提升成绩。
- 浏览器实际操作验证：自动反馈、错题补练、“我 / 宝宝”档案隔离、棋盘与文本恢复、刷新与服务重启后保留档案。620px 视窗无水平溢出，81 个交叉点均可操作。
- 浏览器通过真实本地 KataGo 验证自动应手、执白开局、成对悔棋；双人对局、双方共享历史及恢复通过。
- LLM 使用隔离的模拟 OpenAI 兼容接口，验证连接测试、自动讲解及刷新后保留讲解。真实服务未配置，未验证真实供应商响应。
- 正式服务 127.0.0.1:8769 已升级，迁移保留旧棋盘与着手；旧状态另存 .local/state.pre-profiles.json。没有从旧棋局推断学习成绩，LLM 初始关闭。

学习水平是练习表现指标，不是围棋段位；终局自动数目与电脑重启自启动仍未实现。

## 第三版验证 · 2026-09-07

- 93 项单元／HTTP 测试通过，Python 编译和 JavaScript 语法检查通过。
- 新增 8 个原创多手吃子局面，旋转／镜像归一后仍为 8 个不同布局；3 题含对手不同应手分支，两题为 7 手征吃。每条答案回放验证合法落子和实际提掉目标，倒扑验证先弃一子再提三子。
- 多手练习中间不记成绩，完成后才评分；未收录合法走法不判错。覆盖成对悔棋、重练分支轮换、跨档案恢复、离开未完成题后回来不冒充首次独立作答。
- 导入校验覆盖黑／白先、9／19 路、重复题号拒绝、非法答案拒绝、来源保留、重载持久化；网页/API 状态不泄漏答案树。
- Playwright 实际点击验证：三手双打吃、重试时白应变化、未收录走法待复核、七手征吃完成、9 路 JSON 导入并刷新恢复。
- 19 路实际点击验证：361 个交叉点、T1 与 T19 落子、SZ[19] SGF、19 路白先导入题提子、真实 KataGo 自动应手及刷新恢复。620px 视窗与放大模式均无全页横向溢出，放大棋盘可在容器内滚动。
- 正式 8769 服务已更新，72 个可练版本（64 基础变式＋8 多手原创）可读取；服务重启前后 profiles.json 内容一致，旧学习档案保留，本次测试题仅在隔离目录。

截图：19 路人机对弈（本地验证产物 `output/playwright/v3-19-ai.png`）、19 路小窗口放大（本地验证产物 `output/playwright/v3-19-narrow.png`）、多手吃子完成（本地验证产物 `output/playwright/v3-tactics.png`）。

边界：照片识别需把图发给 Codex，目前未收到真实书题图片；导入支持吃子目标，尚不支持通用做活／破眼评分。答案树校验不证明全部防守或唯一最优解。

截图和测试运行数据仅留在本机，不随源码仓库发布。

## 家庭云端版验证 · 2026-09-07

- 125 项 Python 测试、29 项 Node／SQLite 测试通过；包括 HTTP 边界、实际 SQL 事务 CAS、登录限速、密钥加密、多标签档案一致性、结果只计一次和主备计算切换。
- 原生浏览器／Wrangler 集成：家庭登录、每设备独立档案、并发落子 200／409、双方确认后一次更新 Elo、同浏览器旧档案标签页提交 409、19 路触屏预览确认及 390px 页面无溢出。
- GoGameGuru 固定版本归档 420 原题，417 启用；所有启用题的作者正解树逐条回放通过。云端可选 489 题，3 道旧题号继续兼容但不重复列入选择器。许可、作者与原题链接可在来源行查看。
- Cloudflare Workers／D1／自定义域名已真实部署。家庭生产库由 CLI 迁入 2 个档案、6 次作答、1 盘对局，并回读确认；正式测试不制造新成绩。本机原档案保留，照片与 LLM 配置未迁移。
- 生产浏览器只读核验：489 题与原记录可读；manifest、SW、PNG 均为正确 MIME；SW 仅缓存 9 个公开静态 URL，无 API 缓存；退出后会话失效、私密 DOM 清空，未发现 JavaScript 异常。
- fnOS 主力运行官方 Eigen AVX2 KataGo v1.18.1 + b18，4 搜索线程、单分析队列、最多 64 visits／3 秒；CPU 基准热请求约 3.15 秒、冷启动 4.55 秒、峰值 547 MiB。多数请求先触发时间上限，约 36–47 visits。
- VPS 后备运行相同引擎 + 扩展训练 b10，2 搜索线程、单分析队列；CPU 基准热请求约 1.4 秒、冷启动 2.15 秒、峰值 100 MiB；两条不同请求排队约 2.88 秒。性能不等于人类段位标定。
- 公网实际主备演练：正常 fnOS 返回 200（2.85 秒）；只暂停本项目 fnOS 引擎后 VPS 返回 200（3.17 秒）；恢复引擎后下一次请求回到 fnOS 并返回 200（9.58 秒，含冷启动和网络）。全过程只读分析，正式棋局未增加着手。
- 两端均通过独立鉴权 Tunnel，桥接只监听回环地址；健康探测不启动引擎。fnOS Docker `unless-stopped` 与 VPS systemd enabled 已配置，服务停止／恢复路径已验证；没有为验收重启整台 NAS 或 VPS，因此不把配置等同于整机重启实测。
- 线上发现 Workers 不支持 `redirect: error`，现采用 `manual` 并显式拒绝跳转，令牌不转发到其他地址；引擎与 LLM 的 302 拒绝路径有回归测试。CLI 使用明确的 `Go-Coach-CLI/1.0` User-Agent，通过真实 Cloudflare API 路径。

边界：LLM 真实供应商仍未配置，仍由用户自行启用；书题原图尚未提供；离线 PWA 不提交或排队重放棋步；正式手机／iPad 的“添加到主屏幕”安装尚需在实际设备完成。练习等级和站内 Elo 不代表正式段位，终局仍不自动裁决死活和数目。

最终人机浏览器验收：隔离的本地 Worker 使用真实 fnOS 计算服务，19 路人下 D4 后仅发送一次 `ai_move`，棋局共 2 手，状态和页面均显示 fnOS 主力；生产只读分析返回 200／fnOS，来源许可链接可用，棋盘及 revision 未变。测试浏览器已关闭，本次未改变 Mac 正式本地 8769 服务。

## 练习偏离答案后继续对弈 · 2026-09-12

- 练习首次答错或走出参考答案树后进入持久化续弈：保留首次评分／复核，随后 KataGo 按当前局面自动应手；答案树内仍优先使用作者收录应手。续弈支持继续落子、成对悔棋、停一手和双方双停，不重复写作答记录或 XP。
- 云端 62 项测试、相关本地 Python 57 项测试通过；覆盖基础题答错、自定义摆题、连续题未收录走法、首次记录唯一、review 后只调用 move、旧状态与历史快照恢复、非法续弈标志、引擎失败、并发 CAS、停手和悔棋。Python 编译、JavaScript 语法和 `git diff --check` 通过。
- Playwright 使用隔离数据和真实本地 KataGo：A9 后白 F4，继续 B9 后白 G5；390×844 手机宽度下“停一手”可见且可点击，停手后 KataGo 继续应手。最终 6 手、`lesson_playout=true`、首次练习记录仍为 1 条。
- Cloudflare 生产版本 `b2862f4f-1e2f-4597-9aa9-ce011fa46787` 已部署；`/api/health` 返回 200，线上 `app.js` 与本地 SHA-256 一致，生产只读 `/api/analyze` 返回 200、由 fnOS 主力实际完成并给出 6 个候选点。
- 浏览器测试没有登录生产页面或提交棋步；生产分析只读且 revision 未变。隔离服务和浏览器已关闭，临时练习数据移入废纸篓。控制台只有本地版用于区分云端的既有 `/api/session` 404。

## iPad 连接恢复与低延迟应手 · 2026-09-12

- 前端棋盘状态、动作和模型请求增加分类超时；普通动作 15 秒，AI 应手 22 秒，显式复核 30 秒。POST 超时、无效响应或 5xx 后先同步最新 revision，不盲目重放落子；Safari 回到前台时立即同步，常规轮询改为 12 秒。一次失败先显示“网络波动，正在重连”，连续失败或系统明确离线才显示连接中断。PWA 公共缓存升级为 v8。
- Worker 的实战 `/move` 优先直接请求 VPS 快速通道，不先做健康探测；VPS 失败后只回退一次 fnOS，两段预算分别封顶 8/10 秒。`/analyze` 和显式 `/review` 仍优先 fnOS，健康结果 `busy=true` 时改用 VPS。下一次请求仍从该任务的优先通道开始，不形成永久降级。
- 练习首次偏离答案时仍立即做规则目标和作者反驳核对；两者无法裁定时直接保存落子并进入续弈，不在关键路径调用重型 `/review`。浏览器请求序列实测只有 `play` 和 `ai_move`；只有点击“重新复核”才调用 `review_move`。续弈后只更新当前局面和规则反馈，不相对原答案树重复计分。
- 本地引擎分析锁最多等待 2 秒，普通查询最多等待 8 秒，不无限排队；`/health` 返回 `busy`。fnOS `/vol2/1000/go-coach-engine` 与 VPS `/opt/go-coach-engine` 已更新，两端 `engine.py` SHA-256 为 `bb452dca7f14e7f01fde22daa9302c99c8fbc8c0c0b5abc840b2cc2329a854fd`，`engine_bridge.py` 为 `eeeebf290f6bfd116b4989d677a26767fce0d3dc3936c832bfe60d2a56406e43`；服务重建／重启后两端 `/health` 均返回 200、`available=true`、`busy=false`。本轮未重启整台 NAS 或 VPS，不将服务重启等同于整机重启持久性验收。
- 820×1180 Chromium 移动视口中，第一次状态请求失败后显示正在重连，紧接成功即恢复已连接；POST 挂起测试约 15.7 秒解除 busy 并对账，没有重放落子；模拟回到前台后 313 ms 内发出状态同步。`ggg-easy-01` 在 A19 偏离答案的真实本地 KataGo 实测为：`play` 40 ms，`ai_move` 2256 ms，点击至解锁 2343 ms，页面显示“KataGo 已应手，请继续下一手”。这是 Chromium 移动视口，不是真实 iPad Safari 验收。
- 完整 Python 测试 158/158 通过（905.870 秒），云端 Node 测试 63/63 通过；Python 编译、JavaScript 语法和 `git diff --check` 通过。覆盖 VPS 快速应手无健康往返、VPS 失败回退 fnOS、忙状态回退、双通道超时上界、首次偏离不自动复核、显式复核、续弈不重复计分和并发 CAS。
- Cloudflare 生产版本 `85bec59d-125e-457e-8183-56f5ba5fed26` 已部署；`/api/health` 返回 200。线上 `app.js`、`style.css`、`sw.js` 与本地逐字节一致，SHA-256 分别为 `b231d2a1640cbdeffe420f3558538a43ca81980d4d045c9bda0f29582e188612`、`37833cfb8d8c50381677f21fe9cbc6056bc09c77f079f9e7e5d79ec76e11e17f`、`846bb4ff6e742c3518d85fd7fbc4e67647b61f7102f71281ecfad4ad0ce83752`。生产 `/api/analyze` 连续三次均由 fnOS 完成，分别 6.897、2.755、1.783 秒，每次返回 6 个候选；验证前后 revision 和完整状态一致。VPS `/move` 用合成空盘直连只读测时为 2.901 秒和 0.597 秒，均返回有效应手。

生产验收为保护家庭练习记录，没有在正式棋盘调用会写入的 `ai_move`，因此不声称已完成生产 Worker `/move` 的端到端落子验收。线上版本、VPS 快速通道、Worker 路由测试和本地移动视口续弈均已分别验证；最终 iPad Safari 的实际前后台恢复和手感仍需在真实设备上确认。

# 书题导入

在当前 Codex 对话发送清晰的题目照片；可同时提供答案图、书名、页码、题号与黑先／白先。识别在收到照片后进行，网页本身不含图片识别器。

1. 识别棋子与题意，确认棋盘边界、先行方、目标棋块。看不清的棋子先核对，不猜测后当成确定事实保存。
2. 按原题保留 9 路或 19 路棋盘。必须保留真实边角与影响气、连接、征子的相关棋子；不裁掉关键棋子强行缩成小棋盘。
3. 录入答案页的应手和分支，逐手核对；没有答案时只能录入已验证的变化，不声称穷尽所有防守。
4. 生成下述 JSON，用网页选择文件导入，或经现有本地／家庭云端 `/api/lessons/import`。题库保存后可直接选题。

## 格式

单题 JSON 对象：

- `id`：唯一英文数字编号，可含 `-`、`_`，不能覆盖已有题号。
- `title`、`prompt`、`hint`：题名、题意、提示文字。
- `size: 9 或 19`：棋盘尺寸；省略时按 9 路校验。19 路必须明确填写 19。
- `skill: "capture"`、`sequence: true`、`difficulty: 1..5`、`to_play: 1 或 2`。
- `stones: [{"x": 0, "y": 0, "color": 2}, ...]`。坐标左上为 `[0,0]`（9 路 A9、19 路 A19），19 路右下 T1 为 `[18,18]`，字母跳过 I；颜色 1 黑、2 白。
- `objective: {"kind": "capture", "targets": [[0,0], ...]}`：提掉所有指定初始对方棋子。`capture_any` 为提掉其中任一颗，适用于双打吃。
- `marks: [{"x":0,"y":0,"label":"1"}]`：可选棋盘标记。
- `source: {"kind":"book","title":"书名","page":"页码","problem":"题号","note":"核对情况"}`：只保存文字来源，不自动读取链接或上传原图。原创题用 `original`，手动题用 `manual`。
- `tree: {"children": [...]}`：根不含落子。每个后续节点为 `{"move":[x,y],"explanation":"本步讲解","children":[...]}`；从先行方交替落子，叶节点 `children: []`。同一父节点的不同孩子表示该方可选分支。

提子题每条叶分支必须由先行方实际提掉目标结束；非提子作者答案见下文家庭私用书题格式。校验上限为 31 手、256 节点、每节点 16 分支。初始无气棋块、非法落子、重复节点走法、循环引用、目标未完成均拒绝导入。该校验验证给定变化，不证明最优解或所有防守；未收录走法会提示待复核。

客户端选择文件上限 240 KiB。服务端 JSON 请求上限 256 KiB。本地导入保存到 `.local/imported-lessons.json`；云端导入经家庭登录后保存到 D1 家庭私有题库，同家庭学习者共用题目、分别记录成绩。网页状态和题库列表不返回答案树。


## 家庭私用书题的作者答案

书上答案不以实际提子结束时，使用 `skill: "tsumego"`、`objective: {"kind":"authored_solution"}`。来源必须保持 `kind: "book"`，并同时提供 `usage: "household_private"`、布尔值 `answer_verified: true` 和非空 `title`、`page`、`problem`；`note` 记录答案页码及核对情况。只有逐题核对题图、先行方和答案变化后，才设置核对标记；模糊题留待核对，不进入可练题库。不得为私人书题编造开放许可或设为 `licensed`。

```json
{"kind":"book","usage":"household_private","answer_verified":true,"title":"书名","page":"题目页码","problem":"单元及题号","note":"答案页码；棋盘及答案已逐手核对"}
```

作者答案每个叶节点必须明确写 `result: "success"`、`author_verdict: "correct"`、`children: []`（也接受 `correct: true`）。双方按先行方交替落子，允许正确答案的多个分支，全部逐手合法回放。完成表示达到收录的作者答案，不是程序独立证明死活或穷尽对手防守。错误图不放进正确解树；目前不支持导入失败叶，未收录走法继续待复核。

源 PDF、照片、裁图及私有结构化题目留在仓库外或已忽略的 `.local/`，不放到 GitHub、`static/` 或内置公共题库。云端只接收经核对的结构化题目；家庭私用标记用于保留来源语义，访问隔离由现有家庭认证和 D1 household_id 实现。

使用 `python3 scripts/publish_lesson.py --file /absolute/private/lesson.json --dry-run` 本地校验；核对完成后添加 `--reviewed --password-file /absolute/private/password-file` 发布并回读。脚本发布单题时获取最新 revision 与学习者标识。云端 API 也支持以 `lessons: [...]` 批量导入，每批 1–64 题，总请求仍受 256 KiB 限制；批内全部先校验，以一次事务写入，任一非法题、重号或并发冲突均不写入该批。批量字段与单题 `lesson` 不能同时提交。遇到冲突先回读，不覆盖旧题。云端家庭自定义题最多 2048 道，单次导入请求最多 256 KiB；跨本地和云端录题遵守较小上限：31 手、256 节点、每节点16分支、40个目标或标记。

题库会按 `source.kind=book` 与书名提供“书本练习”筛选；选书后显示该书的全部难度，仍可再按知识点或难度缩小范围。

在菜单选择“顺序练习”后，可按入门课程、Go Game Guru 或书名进入对应章节首题。入门课程按基础到征吃推进，不混入 Go Game Guru；第三方题库按基础、进阶、挑战接续；书本顺序池只包含同一本书，按原题号推进并接续下一章。完成一套题后不会跳到另一套。切换章节不清除已有学习成绩，智能推荐和错题复习继续使用原规则。
